# fake-news-classifier
Fake News Tweet Classifier.  This project implements a ML pipeline to classify tweets as fake or real. Data from multiple CSV files is combined into one dataset and preprocessed using NLP techniques such as link removal, tokenization, stemming, and stopword removal. The text is vectorised and used to train a TensorFlow neural network model.
